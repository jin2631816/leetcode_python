class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        
        j = 0
        for i in nums:
            if val != i:
                nums[j] = i
                j = j + 1
